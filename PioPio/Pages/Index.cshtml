@page
@model IndexModel
@{
    ViewData["Title"] = "Home Page";
}

<script src="~/_framework/blazor.server.js"></script>

@foreach (var tweet in Model.Tweets)
{
    <component type="typeof(Components.Tweet)" param-State="@tweet" render-mode="ServerPrerendered" />
}